{
  "$schema": "http://json-schema.org/schema",
  "id": "SchematicForNewReduxModule",
  "title": "Schema per la generazione di un modulo redux",
  "type": "object",
  "properties": {
    "config": {
      "type": "string",
      "description": "file .yaml di configurazione del business object"
    }
  },
  "required": ["config"]
}